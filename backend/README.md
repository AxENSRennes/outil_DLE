# Backend

This directory is reserved for the future Django + DRF backend.

## Current State

- quality tooling is already wired from the repository root
- `tests/test_smoke.py` exists only to keep the Python toolchain verifiable
- no real Django project has been scaffolded yet

## Intended Use

When implementation starts for real, this directory should contain:

- the Django project
- application packages
- backend tests
- backend-only support modules

Until then, treat this directory as a bootstrap placeholder, not as final project structure.
